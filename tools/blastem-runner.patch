diff --git a/Makefile b/Makefile
--- a/Makefile
+++ b/Makefile
@@ -281,7 +281,7 @@ else
 RENDEROBJS+= $(LIBZOBJS) png.o
 endif
 
-COREOBJS:=system.o genesis.o vdp.o io.o romdb.o hash.o xband.o realtec.o i2c.o nor.o $(M68KOBJS) \
+COREOBJS:=system.o genesis.o vdp.o io.o romdb.o hash.o xband.o realtec.o i2c.o nor.o megaldoom_runner.o $(M68KOBJS) \
 	sega_mapper.o multi_game.o megawifi.o $(NET) serialize.o $(TERMINAL) $(CONFIGOBJS) gst.o \
 	$(TRANSOBJS) $(AUDIOOBJS) saves.o jcart.o gen_player.o coleco.o pico_pcm.o ymz263b.o \
 	segacd.o lc8951.o cdimage.o cdd_mcu.o cd_graphics.o cdd_fader.o sft_mapper.o mediaplayer.o \
diff --git a/blastem.c b/blastem.c
--- a/blastem.c
+++ b/blastem.c
@@ -31,6 +31,7 @@
 #include "zip.h"
 #include "cdimage.h"
 #include "event_log.h"
+#include "megaldoom_runner.h"
 #ifndef DISABLE_NUKLEAR
 #include "nuklear_ui/blastem_nuklear.h"
 #endif
@@ -427,6 +428,17 @@ int main(int argc, char ** argv)
 	uint8_t debug_target = 0;
 	char *port;
 	for (int i = 1; i < argc; i++) {
+		if (!strcmp(argv[i], "--md-route") || !strcmp(argv[i], "--md-waypoints") || !strcmp(argv[i], "--md-report") || !strcmp(argv[i], "--md-capture-dir")) {
+			if (++i >= argc) fatal_error("%s needs a value\n", argv[i-1]);
+			if (!strcmp(argv[i-1], "--md-route")) megaldoom_route_load(argv[i]);
+			else if (!strcmp(argv[i-1], "--md-waypoints")) megaldoom_waypoints_load(argv[i]);
+			else if (!strcmp(argv[i-1], "--md-report")) megaldoom_runner_set_report(argv[i]);
+			else megaldoom_runner_set_capture(argv[i], 1);
+			continue;
+		}
+		if (!strcmp(argv[i], "--md-capture-every")) { if (++i >= argc) fatal_error("--md-capture-every needs a value\n"); megaldoom_runner_set_capture_every(atoi(argv[i])); continue; }
+		if (!strcmp(argv[i], "--md-mailbox")) { uint32_t address, bytes = 128; if (++i >= argc || sscanf(argv[i], "%x:%u", &address, &bytes) < 1) fatal_error("--md-mailbox needs ADDRESS[:BYTES]\n"); megaldoom_runner_set_mailbox(address, bytes); continue; }
+		if (!strcmp(argv[i], "--md-perf-mailbox")) { uint32_t address, bytes = 128; if (++i >= argc || sscanf(argv[i], "%x:%u", &address, &bytes) < 1) fatal_error("--md-perf-mailbox needs ADDRESS[:BYTES]\n"); megaldoom_runner_set_perf_mailbox(address, bytes); continue; }
+		if (!strcmp(argv[i], "--md-require-checkpoints")) { uint32_t mask; if (++i >= argc || sscanf(argv[i], "%x", &mask) != 1) fatal_error("--md-require-checkpoints needs a hex MASK\n"); megaldoom_runner_set_require_checkpoints(mask); continue; }
 		if (argv[i][0] == '-') {
 			switch(argv[i][1]) {
 			case 'b':
@@ -779,5 +791,6 @@ int main(int argc, char ** argv)
 	}
 #endif //__EMSCRIPTEN__
 
+	megaldoom_runner_finish();
 	return 0;
 }
diff --git a/genesis.c b/genesis.c
--- a/genesis.c
+++ b/genesis.c
@@ -22,6 +22,7 @@
 #include "radica.h"
 #include "config.h"
 #include "event_log.h"
+#include "megaldoom_runner.h"
 #include "paths.h"
 #define MCLKS_NTSC 53693175
 #define MCLKS_PAL  53203395
@@ -599,6 +600,7 @@ static m68k_context *sync_components(m68k_context * context, uint32_t address)
 		//printf("reached frame end %d | MCLK Cycles: %d, Target: %d, VDP cycles: %d, vcounter: %d, hslot: %d\n", gen->last_frame, mclks, gen->frame_end, v_context->cycles, v_context->vcounter, v_context->hslot);
 		uint32_t elapsed = v_context->frame - gen->last_frame;
 		gen->last_frame = v_context->frame;
+		megaldoom_runner_on_frame(&gen->header, gen->last_frame, mclks);
 		event_flush(mclks);
 		gen->last_flush_cycle = mclks;
 		if (gen->header.enter_debugger_frames) {
@@ -612,6 +614,7 @@ static m68k_context *sync_components(m68k_context * context, uint32_t address)
 
 		if(exit_after){
 			if (elapsed >= exit_after) {
+				megaldoom_runner_finish();
 				exit(0);
 			} else {
 				exit_after -= elapsed;
@@ -876,6 +879,7 @@ static m68k_context* sync_components_pico(m68k_context * context, uint32_t addre
 		//printf("reached frame end %d | MCLK Cycles: %d, Target: %d, VDP cycles: %d, vcounter: %d, hslot: %d\n", gen->last_frame, mclks, gen->frame_end, v_context->cycles, v_context->vcounter, v_context->hslot);
 		uint32_t elapsed = v_context->frame - gen->last_frame;
 		gen->last_frame = v_context->frame;
+		megaldoom_runner_on_frame(&gen->header, gen->last_frame, mclks);
 		event_flush(mclks);
 		gen->last_flush_cycle = mclks;
 		if (gen->header.enter_debugger_frames) {
@@ -889,6 +893,7 @@ static m68k_context* sync_components_pico(m68k_context * context, uint32_t addre
 
 		if(exit_after){
 			if (elapsed >= exit_after) {
+				megaldoom_runner_finish();
 				exit(0);
 			} else {
 				exit_after -= elapsed;
diff --git a/megaldoom_runner.c b/megaldoom_runner.c
new file mode 100644
--- /dev/null
+++ b/megaldoom_runner.c
@@ -0,0 +1,152 @@
+#include "megaldoom_runner.h"
+#include "genesis.h"
+#include "io.h"
+#include "ppm.h"
+#include "util.h"
+#include "vdp.h"
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <math.h>
+#ifndef M_PI
+#define M_PI 3.14159265358979323846
+#endif
+
+#define MEGALDOOM_RUNNER_VERSION 4
+#define MD_MASK_UP    0x001
+#define MD_MASK_LEFT  0x004
+#define MD_MASK_RIGHT 0x008
+#define MD_MASK_A     0x010
+#define MD_MASK_B     0x020
+#define MD_MASK_C     0x040
+#define E2E_EVENT_STARTED    0x01
+#define E2E_EVENT_COMBAT_HIT 0x04
+#define E2E_EVENT_EXIT       0x80
+
+typedef enum { WAYPOINT_MOVE, WAYPOINT_USE, WAYPOINT_FIRE, WAYPOINT_HURT, WAYPOINT_EXIT } waypoint_action;
+typedef struct { int16_t x, y, aim_x, aim_y; uint16_t radius, timeout; uint8_t action, event_mask, expected_action, expected_target; } waypoint;
+
+typedef struct {
+    uint32_t *masks, count, active, cycles_first, cycles_last;
+    uint32_t pc[64];
+    char *report, *capture;
+    uint32_t capture_every, capture_failed;
+    uint32_t mailbox, mailbox_bytes;
+    char mailbox_hex[1025];
+    uint32_t perf_mailbox, perf_mailbox_bytes;
+    char perf_mailbox_hex[1025];
+    uint32_t require_checkpoints, checkpoints_seen;
+    waypoint *waypoints;
+    uint32_t waypoint_count, waypoint_index, waypoint_started;
+    uint32_t waypoint_complete, waypoint_failed, waypoint_use_pulse, waypoint_use_serial, waypoint_use_pulses;
+    char waypoint_failure[96];
+    uint8_t mailbox_raw[512];
+} runner_state;
+static runner_state state;
+
+static char *copy_string(const char *value) { size_t n = strlen(value) + 1; char *out = malloc(n); if (out) memcpy(out, value, n); return out; }
+static void set_string(char **out, const char *value) { free(*out); *out = copy_string(value); }
+
+/* Each line is a (frame, mask) edge: the mask takes effect at `frame` and
+ * holds for every subsequent frame until the next line changes it (a route
+ * that presses a button at frame 10 and releases it at frame 50 only needs
+ * those two lines, not one per held frame). Without the forward-fill below,
+ * any frame not explicitly listed defaulted to 0 (via the memset on growth),
+ * so a held button was released again on the very next frame -- set_input()
+ * saw mask 0 versus a previous mask with the bit set and called gamepad_up
+ * immediately, one frame after the gamepad_down. */
+void megaldoom_route_load(const char *path)
+{
+    FILE *file = fopen(path, "r"); uint32_t frame, mask, capacity = 0;
+    uint32_t prev_frame = 0, prev_mask = 0; int have_prev = 0;
+    if (state.waypoints) fatal_error("--md-route and --md-waypoints cannot be combined\n");
+    if (!file) fatal_error("Could not open deterministic route %s\n", path);
+    while (fscanf(file, "%u %x", &frame, &mask) == 2) {
+        if (frame >= capacity) { uint32_t next = capacity ? capacity : 256; while (frame >= next) next *= 2; state.masks = realloc(state.masks, sizeof(*state.masks) * next); if (!state.masks) fatal_error("Out of memory loading route\n"); memset(state.masks + capacity, 0, sizeof(*state.masks) * (next - capacity)); capacity = next; }
+        if (have_prev) { for (uint32_t f = prev_frame; f < frame; f++) state.masks[f] = prev_mask; }
+        state.masks[frame] = mask; state.count = frame + 1;
+        prev_frame = frame; prev_mask = mask; have_prev = 1;
+    }
+    fclose(file);
+}
+/* X Y AIM_X AIM_Y ARRIVAL_RADIUS ACTION EVENT_MASK EXPECTED_ACTION EXPECTED_TARGET TIMEOUT.
+ * Eight-column v1 files remain accepted, but generated USE lines declare the
+ * expected gameplay result and target identity. */
+void megaldoom_waypoints_load(const char *path)
+{
+    FILE *file = fopen(path, "r"); char line[256], action[16];
+    uint32_t capacity = 0, radius, event_mask, timeout; int x, y, aim_x, aim_y, expected_action = -1, expected_target = -1;
+    if (state.masks) fatal_error("--md-route and --md-waypoints cannot be combined\n");
+    if (!file) fatal_error("Could not open waypoint route %s\n", path);
+    while (fgets(line, sizeof(line), file)) {
+        waypoint_action parsed; char *start = line;
+        while (*start == ' ' || *start == '\t') start++;
+        if (!*start || *start == '#' || *start == '\n') continue;
+        int fields = sscanf(start, "%d %d %d %d %u %15s %x %d %d %u", &x, &y, &aim_x, &aim_y, &radius, action, &event_mask, &expected_action, &expected_target, &timeout);
+        if (fields == 8) { if (sscanf(start, "%d %d %d %d %u %15s %x %u", &x, &y, &aim_x, &aim_y, &radius, action, &event_mask, &timeout) != 8) fields = 0; expected_action = expected_target = -1; }
+        if ((fields != 8 && fields != 10) || radius == 0 || timeout == 0 || x < -32768 || x > 32767 || y < -32768 || y > 32767 || aim_x < -32768 || aim_x > 32767 || aim_y < -32768 || aim_y > 32767 || event_mask > 0xFF || expected_action > 255 || expected_target > 255) fatal_error("Invalid waypoint in %s: %s", path, line);
+        if (!strcmp(action, "MOVE")) parsed = WAYPOINT_MOVE;
+        else if (!strcmp(action, "USE")) parsed = WAYPOINT_USE;
+        else if (!strcmp(action, "FIRE")) parsed = WAYPOINT_FIRE;
+        else if (!strcmp(action, "HURT")) parsed = WAYPOINT_HURT;
+        else if (!strcmp(action, "EXIT")) parsed = WAYPOINT_EXIT;
+        else fatal_error("Unknown waypoint action %s in %s\n", action, path);
+        if (state.waypoint_count == capacity) { uint32_t next = capacity ? capacity * 2 : 32; waypoint *grown = realloc(state.waypoints, sizeof(*grown) * next); if (!grown) fatal_error("Out of memory loading waypoint route\n"); state.waypoints = grown; capacity = next; }
+        waypoint *out = &state.waypoints[state.waypoint_count++];
+        out->x = (int16_t)x; out->y = (int16_t)y; out->aim_x = (int16_t)aim_x; out->aim_y = (int16_t)aim_y; out->radius = (uint16_t)radius; out->action = (uint8_t)parsed; out->event_mask = (uint8_t)event_mask; out->expected_action = fields == 10 ? (uint8_t)expected_action : 0xFF; out->expected_target = fields == 10 ? (uint8_t)expected_target : 0xFF; out->timeout = (uint16_t)timeout;
+    }
+    fclose(file); if (!state.waypoint_count) fatal_error("Waypoint route %s is empty\n", path);
+}
+void megaldoom_runner_set_report(const char *path) { set_string(&state.report, path); }
+void megaldoom_runner_set_mailbox(uint32_t address, uint32_t bytes) { state.mailbox = address; state.mailbox_bytes = bytes > 512 ? 512 : bytes; }
+void megaldoom_runner_set_perf_mailbox(uint32_t address, uint32_t bytes) { state.perf_mailbox = address; state.perf_mailbox_bytes = bytes > 512 ? 512 : bytes; }
+void megaldoom_runner_set_require_checkpoints(uint32_t mask) { state.require_checkpoints = mask; }
+void megaldoom_runner_set_capture(const char *directory, uint32_t every) { set_string(&state.capture, directory); state.capture_every = every; }
+void megaldoom_runner_set_capture_every(uint32_t every) { state.capture_every = every; }
+
+static void set_input(system_header *system, uint32_t mask)
+{
+    static uint32_t previous; static const uint8_t buttons[] = {DPAD_UP, DPAD_DOWN, DPAD_LEFT, DPAD_RIGHT, BUTTON_A, BUTTON_B, BUTTON_C, BUTTON_START, BUTTON_X, BUTTON_Y, BUTTON_Z, BUTTON_MODE};
+    for (uint8_t i = 0; i < sizeof(buttons); i++) { uint32_t bit = 1u << i; if ((mask & bit) != (previous & bit)) { if (mask & bit) system->gamepad_down(system, 1, buttons[i]); else system->gamepad_up(system, 1, buttons[i]); } }
+    previous = mask;
+}
+
+/* Reads `bytes` from Genesis work RAM at `address` into `hex_out` (a
+ * 2*bytes+1 char buffer) as lowercase hex, and returns the first byte raw
+ * (used for checkpoint-bit accumulation) or -1 if the range is invalid/out
+ * of bounds. Shared by the generic mailbox and the perf mailbox so both
+ * follow the same bounds checking. */
+static int read_mailbox(genesis_context *gen, uint32_t address, uint32_t bytes, char *hex_out, uint8_t *raw_out)
+{
+    static const char hex[] = "0123456789abcdef";
+    uint32_t offset;
+    if (address < 0xFF0000 || address >= 0x1000000 || !bytes) return -1;
+    offset = address - 0xFF0000;
+    if (offset + bytes > RAM_WORDS * 2) return -1;
+    for (uint32_t i = 0; i < bytes; i++) {
+        uint8_t value = ((uint8_t *)gen->work_ram)[(offset + i) ^ 1];
+        if (raw_out) raw_out[i] = value;
+        hex_out[i * 2] = hex[value >> 4];
+        hex_out[i * 2 + 1] = hex[value & 15];
+        if (i == 0) hex_out[bytes * 2] = 0;
+    }
+    return ((uint8_t *)gen->work_ram)[offset ^ 1];
+}
+static int16_t mailbox_s16(const uint8_t *raw, uint32_t offset) { return (int16_t)(((uint16_t)raw[offset] << 8) | raw[offset + 1]); }
+static uint16_t mailbox_u16(const uint8_t *raw, uint32_t offset) { return (uint16_t)(((uint16_t)raw[offset] << 8) | raw[offset + 1]); }
+static void waypoint_fail(const char *reason) { if (!state.waypoint_failed) { snprintf(state.waypoint_failure, sizeof(state.waypoint_failure), "%s", reason); state.waypoint_failed = 1; } }
+static void waypoint_next(void) { state.waypoint_index++; state.waypoint_started = 0; state.waypoint_use_pulse = state.waypoint_use_serial = 0; if (state.waypoint_index == state.waypoint_count) state.waypoint_complete = 1; }
+static uint32_t waypoint_turn(waypoint *current, int16_t px, int16_t py, uint16_t angle) { int32_t aim_dx = (int32_t)current->aim_x - px, aim_dy = (int32_t)current->aim_y - py; if (current->action == WAYPOINT_MOVE && aim_dx == 0 && aim_dy != 0) return 0; double desired = atan2((double)aim_dy, (double)aim_dx) * (256.0 / (2.0 * M_PI)); int32_t error = ((int32_t)desired - angle) & 255; if (error > 127) error -= 256; /* The accelerated turn controller needs a small dead-band or it hunts around the target heading forever. */ if (error >= -8 && error <= 8) return 0; return error > 0 ? MD_MASK_RIGHT : (error < 0 ? MD_MASK_LEFT : 0); }
+static uint32_t waypoint_input(uint32_t frame)
+{
+    if (!state.waypoints || state.waypoint_complete || state.waypoint_failed) return 0;
+    if (!(state.mailbox_raw[0] & E2E_EVENT_STARTED)) return 0;
+    if (mailbox_u16(state.mailbox_raw, 18) == 0xFFFF) return 0;
+    if (mailbox_s16(state.mailbox_raw, 14) == -32768 || mailbox_s16(state.mailbox_raw, 16) == -32768) return 0;
+    waypoint *current = &state.waypoints[state.waypoint_index]; if (!state.waypoint_started) state.waypoint_started = frame;
+    int16_t px = mailbox_s16(state.mailbox_raw, 14), py = mailbox_s16(state.mailbox_raw, 16); int32_t dx = (int32_t)current->x - px, dy = (int32_t)current->y - py;
+    if (frame - state.waypoint_started > current->timeout) { snprintf(state.waypoint_failure, sizeof(state.waypoint_failure), "timeout wp=%u action=%u pose=%d,%d,%u use=%u/%u result=%u:%u", state.waypoint_index, current->action, px, py, mailbox_u16(state.mailbox_raw, 18), state.waypoint_use_serial, state.mailbox_raw[9], state.mailbox_raw[10], state.mailbox_raw[11]); waypoint_fail(state.waypoint_failure); return 0; }
+    uint32_t in_range = (uint32_t)(dx * dx + dy * dy) <= (uint32_t)current->radius * current->radius;
+    if (current->action == WAYPOINT_MOVE && in_range) { waypoint_next(); return 0; }
+    if (current->action == WAYPOINT_USE && in_range) {
+        uint32_t turn = waypoint_turn(current, px, py, mailbox_u16(state.mailbox_raw, 18)); if (turn) return turn;
+        if (!state.waypoint_use_pulse) { state.waypoint_use_pulse = 1; state.waypoint_use_serial = state.mailbox_raw[9]; state.waypoint_use_pulses++; }
+        if (state.mailbox_raw[9] == state.waypoint_use_serial) return MD_MASK_C;
+        if ((current->expected_action != 0xFF && state.mailbox_raw[10] != current->expected_action) || (current->expected_target != 0xFF && state.mailbox_raw[11] != current->expected_target) || ((state.mailbox_raw[0] & current->event_mask) != current->event_mask)) { snprintf(state.waypoint_failure, sizeof(state.waypoint_failure), "wrong use wp=%u expected=%u:%u got=%u:%u", state.waypoint_index, current->expected_action, current->expected_target, state.mailbox_raw[10], state.mailbox_raw[11]); waypoint_fail(state.waypoint_failure); return 0; }
+        waypoint_next(); return 0;
+    }
+    if (current->action == WAYPOINT_FIRE && in_range) { uint32_t turn = waypoint_turn(current, px, py, mailbox_u16(state.mailbox_raw, 18)); if (turn) return turn; if ((state.mailbox_raw[0] & current->event_mask) == current->event_mask) { waypoint_next(); return 0; } return ((frame & 15) < 4) ? MD_MASK_B : 0; }
+    if (current->action == WAYPOINT_HURT) { if (state.mailbox_raw[7]) waypoint_next(); return 0; }
+    if (current->action == WAYPOINT_EXIT) { if ((state.mailbox_raw[0] & current->event_mask) == current->event_mask) waypoint_next(); return 0; }
+    { waypoint travel = *current; travel.aim_x = current->x; travel.aim_y = current->y; if (current->action == WAYPOINT_MOVE && dx == 0) return MD_MASK_UP; uint32_t turn = waypoint_turn(&travel, px, py, mailbox_u16(state.mailbox_raw, 18)); if (turn) return turn; return MD_MASK_UP; }
+}
+
+/* MEGALDOOM_MIN_CAPTURE_FRAME: the VDP hasn't finished a real frame yet on
+ * the very first megaldoom_runner_on_frame call, so a capture requested at
+ * frame 0 would read a framebuffer that's still zeroed/partial. Delay
+ * capture until at least one full frame has been observed. */
+#define MEGALDOOM_MIN_CAPTURE_FRAME 2
+
+/* Deterministic routes always run BlastEm in headless batch mode (-b), where
+ * the VDP renders into a plain malloc'd buffer (see vdp.c's init_vdp_context:
+ * "if (headless) { context->fb = malloc(...); }") and the normal SDL present
+ * path in render_sdl.c -- the only place that ever consumes a path queued via
+ * render_save_screenshot() -- never runs. Calling render_save_screenshot()
+ * here would silently queue a path that's never written; export the VDP
+ * framebuffer directly with BlastEm's own save_ppm() instead. */
+static void request_capture(genesis_context *gen, uint32_t frame)
+{
+    char path[1024];
+    int written = snprintf(path, sizeof(path), "%s/frame-%06u.ppm", state.capture, frame);
+    if (written <= 0 || (size_t)written >= sizeof(path)) {
+        state.capture_failed = 1;
+        fatal_error("Capture path too long for frame %u in %s\n", frame, state.capture);
+    }
+    FILE *file = fopen(path, "wb");
+    if (!file) {
+        state.capture_failed = 1;
+        fatal_error("Could not open capture file %s for frame %u\n", path, frame);
+    }
+    /* context->output_lines is reset to 0 the instant a frame completes (see
+     * vdp.c's frame-boundary handling, "context->output_lines = 0;"), which
+     * happens before control returns up to the genesis.c call site that
+     * invokes megaldoom_runner_on_frame -- by the time we get here it's
+     * already 0. inactive_start/border_top/border_bot are only touched on
+     * mode-register writes, not per frame, so their sum is a stable height
+     * for the currently configured video mode. */
+    uint32_t height = gen->vdp->inactive_start + gen->vdp->border_top + gen->vdp->border_bot;
+    save_ppm(file, gen->vdp->fb, LINEBUF_SIZE, height, gen->vdp->output_pitch);
+    fclose(file);
+}
+
+void megaldoom_runner_on_frame(system_header *system, uint32_t frame, uint32_t cycles)
+{
+    if (!state.active++) state.cycles_first = cycles; state.cycles_last = cycles;
+    if (system->type == SYSTEM_GENESIS) {
+        genesis_context *gen = (genesis_context *)system;
+        int checkpoint_byte;
+        state.pc[(gen->m68k->last_prefetch_address >> 1) & 63]++;
+        checkpoint_byte = read_mailbox(gen, state.mailbox, state.mailbox_bytes, state.mailbox_hex, state.mailbox_raw);
+        /* Checkpoint bits ride on byte 0 of the generic --md-mailbox range
+         * (the game publishes them at a src/debug_checkpoint.c-owned
+         * address); OR across the whole run since bits like MENU are
+         * transient and would otherwise be lost once gameplay starts. */
+        if (checkpoint_byte >= 0) state.checkpoints_seen |= (uint32_t)checkpoint_byte;
+        read_mailbox(gen, state.perf_mailbox, state.perf_mailbox_bytes, state.perf_mailbox_hex, NULL);
+        if (state.masks) set_input(system, frame < state.count ? state.masks[frame] : 0);
+        else if (state.waypoints) set_input(system, waypoint_input(frame));
+        if (state.capture && state.capture_every && frame >= MEGALDOOM_MIN_CAPTURE_FRAME && !(frame % state.capture_every)) {
+            request_capture(gen, frame);
+        }
+    }
+}
+void megaldoom_runner_finish(void)
+{
+    if (!state.report) return;
+    FILE *file = fopen(state.report, "w"); if (!file) return;
+    uint32_t checkpoints_ok = (state.checkpoints_seen & state.require_checkpoints) == state.require_checkpoints;
+    fprintf(file, "{\n  \"schemaVersion\": 2,\n  \"frames\": %u,\n  \"cycles\": %u,\n  \"pcHistogram\": [", state.active, state.cycles_last - state.cycles_first);
+    for (uint32_t i = 0; i < 64; i++) fprintf(file, "%s%u", i ? ", " : "", state.pc[i]);
+    fprintf(file, "],\n  \"captureFailed\": %s,\n  \"mailbox\": \"%s\",\n  \"perfMailbox\": \"%s\",\n  \"checkpoints\": {\"seen\": %u, \"required\": %u, \"ok\": %s},\n  \"waypoints\": {\"enabled\": %s, \"complete\": %s, \"failed\": %s, \"current\": %u, \"total\": %u, \"usePulses\": %u, \"reason\": \"%s\"}\n}\n",
+            state.capture_failed ? "true" : "false", state.mailbox_hex, state.perf_mailbox_hex,
+            state.checkpoints_seen, state.require_checkpoints, checkpoints_ok ? "true" : "false", state.waypoints ? "true" : "false", state.waypoint_complete ? "true" : "false", state.waypoint_failed ? "true" : "false", state.waypoint_index, state.waypoint_count, state.waypoint_use_pulses, state.waypoint_failure);
+    fclose(file);
+}
diff --git a/megaldoom_runner.h b/megaldoom_runner.h
new file mode 100644
--- /dev/null
+++ b/megaldoom_runner.h
@@ -0,0 +1,20 @@
+#ifndef MEGALDOOM_RUNNER_H_
+#define MEGALDOOM_RUNNER_H_
+
+#include <stdint.h>
+#include "system.h"
+
+/* Automation hook intentionally kept out of normal input bindings. Route files
+ * use `frame mask` lines; mask bits map to Up, Down, Left, Right, A, B, C,
+ * Start, X, Y, Z and Mode respectively. */
+void megaldoom_route_load(const char *path);
+void megaldoom_waypoints_load(const char *path);
+void megaldoom_runner_set_report(const char *path);
+void megaldoom_runner_set_mailbox(uint32_t address, uint32_t bytes);
+void megaldoom_runner_set_perf_mailbox(uint32_t address, uint32_t bytes);
+void megaldoom_runner_set_require_checkpoints(uint32_t mask);
+void megaldoom_runner_set_capture(const char *directory, uint32_t every);
+void megaldoom_runner_set_capture_every(uint32_t every);
+void megaldoom_runner_on_frame(system_header *system, uint32_t frame, uint32_t cycles);
+void megaldoom_runner_finish(void);
+
+#endif
